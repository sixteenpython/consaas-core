# Factory backlog

Features describe user and architectural outcomes. Tasks are bounded executable units linked to a feature. Human-readable Markdown is authoritative for intent; `index.json` is the machine-readable index.

Lifecycle: `DRAFT → BACKLOG → READY → IN_PROGRESS → TESTING → REVIEW → DONE`. A blocked item records the blocker rather than pretending progress.

